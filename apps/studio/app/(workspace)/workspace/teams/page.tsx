import { ComingSoonPage } from "../ComingSoonPage";

export default function TeamsPage() {
  return (
    <ComingSoonPage
      title="Teams & Permissions"
      body="Org-level identity: who exists, what role they hold, and which clusters each team can reach."
    />
  );
}
